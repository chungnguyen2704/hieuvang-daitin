/**
 * CẤU HÌNH — sửa SHEET_ID dưới đây sau khi tạo Google Sheet.
 * Hướng dẫn lấy ID: xem README.md mục "Bước 2".
 */
window.CONFIG = {
  // Dán ID Google Sheet của bạn vào đây (lấy từ URL Sheet)
  SHEET_ID: "1dOoUq7-Pyl-PvdIZCN_HNPDmchwRqF-TP4tFqrR9QME",

  // Tên 2 tab trong Sheet — không cần đổi nếu bạn đặt theo template
  PRICES_TAB: "Prices",
  CONFIG_TAB: "Config",

  // Tự động làm mới mỗi N giây (đặt 0 để tắt auto-refresh)
  AUTO_REFRESH_SECONDS: 30
};
